[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Single-page app: serve index.html for routes like /blog/thesis-kickoff
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
